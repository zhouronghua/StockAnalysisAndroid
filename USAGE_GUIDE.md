# 安卓股票分析应用使用指南

## 应用安装

### 方法1: 直接安装APK

1. 将`stockanalysis-1.0.0-debug.apk`传输到手机
2. 打开APK文件
3. 允许"安装未知来源应用"
4. 点击安装

### 方法2: 通过ADB安装（开发者）

```bash
adb install bin/stockanalysis-1.0.0-debug.apk
```

## 首次使用

### 1. 授予权限

应用首次启动时会请求以下权限：

- **存储权限**: 用于保存股票数据
- **网络权限**: 用于下载股票数据
- **后台运行权限**: 用于定时任务

**重要**: 请全部允许，否则应用无法正常工作。

### 2. 下载数据

1. 点击"下载数据"按钮
2. 等待下载完成（首次约30-40分钟）
3. 状态栏会显示下载进度

**注意**: 
- 首次下载约4300只股票的数据
- 需要良好的网络连接
- 建议在WiFi环境下进行
- 下载过程中不要关闭应用

### 3. 查看结果

下载完成后，会自动显示分析结果：

- 股票代码
- 股票名称
- 成交量倍数
- 收盘价

## 主要功能

### 1. 主界面

**功能按钮**:
- **下载数据**: 手动触发数据下载和分析
- **成交量分析**: 查看成交量暴涨分析
- **历史记录**: 查看历史分析结果
- **设置**: 调整分析参数

**股票列表**:
- 显示所有符合条件的股票
- 点击可查看详细信息

### 2. 成交量分析

分析规则：
- 当天成交量 >= 前7天平均成交量的5倍
- 收盘价高于MA120均线

### 3. 历史记录

查看过去30天的分析结果，可以查看历史上符合条件的股票。

### 4. 设置

调整以下参数：
- MA均线周期（默认120天）
- 成交量倍数阈值（默认5倍）
- 定时任务时间（默认15:30）
- 下载并发数（默认1）

## 自动定时任务

### 配置

应用会在每天**15:30**自动执行以下任务：

1. 下载最新交易数据（增量更新）
2. 计算技术指标
3. 筛选符合条件的股票
4. 发送通知

### 电池优化设置

为确保定时任务正常运行，需要：

1. 进入手机"设置"
2. 找到"电池"或"电池优化"
3. 选择"股票分析"应用
4. 设置为"不优化"或"允许后台运行"

**不同品牌手机设置路径**:

- **小米**: 设置 -> 应用设置 -> 应用管理 -> 股票分析 -> 省电策略 -> 无限制
- **华为**: 设置 -> 应用 -> 应用启动管理 -> 股票分析 -> 手动管理 -> 全部开启
- **OPPO**: 设置 -> 电池 -> 应用耗电管理 -> 股票分析 -> 允许后台运行
- **VIVO**: 设置 -> 电池 -> 后台耗电管理 -> 股票分析 -> 允许后台高耗电
- **三星**: 设置 -> 应用程序 -> 股票分析 -> 电池 -> 不受限制

## 数据说明

### 存储位置

应用数据存储在：
```
/storage/emulated/0/StockAnalysis/
├── data/
│   ├── daily/          # 日线数据（约4300个CSV文件）
│   ├── stocks/         # 股票列表
│   └── results/        # 分析结果
├── logs/              # 日志文件
└── config.ini         # 配置文件
```

### 数据量

- 单只股票数据: 约20-30KB
- 全部股票数据: 约80-120MB
- 建议至少保留500MB可用空间

### 数据更新

- **首次运行**: 下载最近150天的数据
- **日常更新**: 只下载最新一天的数据（约2-5分钟）

## 性能优化

### 提升下载速度

1. 使用WiFi网络
2. 在网络空闲时段运行
3. 关闭其他占用网络的应用

### 节省流量

1. 首次下载在WiFi环境完成
2. 日常更新流量很少（约5-10MB/天）
3. 关闭不必要的图表加载

### 节省存储空间

定期清理30天前的历史数据：

设置 -> 数据管理 -> 清理旧数据

## 常见问题

### Q1: 应用闪退怎么办？

A: 
1. 检查是否授予所有必要权限
2. 确保有足够的存储空间（至少500MB）
3. 重新安装应用
4. 查看日志文件：`/storage/emulated/0/StockAnalysis/logs/`

### Q2: 下载失败怎么办？

A:
1. 检查网络连接
2. 检查存储空间
3. 尝试切换WiFi网络
4. 重启应用重试

### Q3: 定时任务不执行？

A:
1. 检查是否授予后台运行权限
2. 关闭电池优化
3. 确保应用未被系统清理
4. 重启手机后重新开启应用

### Q4: 找不到符合条件的股票？

A:
1. 检查数据是否下载完整
2. 查看分析参数设置（可能阈值过高）
3. 确认是交易日（非周末/节假日）
4. 尝试降低成交量倍数阈值

### Q5: 应用占用空间过大？

A:
1. 清理30天前的数据
2. 只保留最近60天的K线数据
3. 删除不必要的日志文件

## 卸载应用

### 常规卸载

设置 -> 应用管理 -> 股票分析 -> 卸载

### 完全清理

卸载后，如需删除所有数据：

使用文件管理器删除：
```
/storage/emulated/0/StockAnalysis/
```

## 隐私说明

- 本应用**不收集**任何个人信息
- 所有数据**仅存储在本地**
- 仅访问BaoStock公开数据接口
- 不上传任何数据到服务器

## 技术支持

如有问题，请查看日志文件：
```
/storage/emulated/0/StockAnalysis/logs/Main_YYYYMMDD.log
```

或提交Issue到项目仓库。

## 免责声明

本应用仅供学习研究使用，不构成投资建议。
股市有风险，投资需谨慎。
